<?xml version="1.0" encoding="UTF-8"?>
<!--导入事务和AOP相关的命名空间-->
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:aop="http://www.springframework.org/schema/aop"
       xmlns:tx="http://www.springframework.org/schema/tx"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
       http://www.springframework.org/schema/beans/spring-beans.xsd
       http://www.springframework.org/schema/context
       http://www.springframework.org/schema/context/spring-context-2.5.xsd
       http://www.springframework.org/schema/tx
       http://www.springframework.org/schema/tx/spring-tx-4.2.xsd
       http://www.springframework.org/schema/aop
       http://www.springframework.org/schema/aop/spring-aop.xsd">

    <!--扫描指定包下面的注解类（包括子包和孙包中的），将这些注解类注册到spring容器-->
    <context:component-scan base-package="com.test"/>
    <!--加载类路径下的db.properties文件-->
    <context:property-placeholder location="classpath:db.properties"/>

    <!--配置数据源-->
    <bean id="dataSource" class="com.mchange.v2.c3p0.ComboPooledDataSource">
        <property name="driverClass" value="${jdbc.driver}"></property>
        <property name="jdbcUrl" value="${jdbc.url}"></property>
        <property name="user" value="${jdbc.username}"></property>
        <property name="password" value="${jdbc.password}"></property>
        <property name="initialPoolSize" value="${jdbc.initialPoolSize}"></property>
        <property name="maxPoolSize" value="${jdbc.maxPoolSize}"></property>
        <property name="maxStatements" value="${jdbc.maxStatements}"></property>
        <property name="acquireIncrement" value="${jdbc.acquireIncrement}"></property>
    </bean>

    <!--mybatis SqlSessionFactory 的工厂bean-->
    <bean id="sqlSessionFactoryBean" class="org.mybatis.spring.SqlSessionFactoryBean">
        <property name="dataSource" ref="dataSource"/>
        <property name="configLocation" value="classpath:mybatis/mybatis-configuration.xml"/>
        dsfs
        <property name="mapperLocations" value="classpath:mybatis"/>
    </bean>

    <!--
    mapper批量扫描，从指定包中mapper接口，自动创建道理对象并且注册到Spring容器中，这些mapper的bean的name是mapper.java的类型，首字母小写；
    遵循的规范：mapper.java和mapper.xml的文件要同名并且在同一目录下。这种情况况下就不用再mybatis的核心文件中配置加载mapper.xml文件了，如果想把mapper.xml
    文件放在其他目录下，可以通过    加载进来
    -->
    <bean class="org.mybatis.spring.mapper.MapperScannerConfigurer">
        <!--指定扫描mapper的包名，如果有多个包中间使用半角逗号分隔-->
        <property name="basePackage" value="com.test.dao"/>
        <!--sqlSessionFactoryBeanName这个属性是String类型，所以使用的是value而不是ref-->
        <property name="sqlSessionFactoryBeanName" value="sqlSessionFactoryBean"/>
    </bean>

    <!--事物管理器-->
    <bean id="txManager" class="org.springframework.jdbc.datasource.DataSourceTransactionManager">
        <property name="dataSource" ref="dataSource"/>
    </bean>

    <!--配置事物增强，拦截到方法之后如何管理事物-->
    <tx:advice id="txAdvice" transaction-manager="txManager">
        <tx:attributes>
            <tx:method name="*" isolation="DEFAULT" propagation="REQUIRED" read-only="false"/>
        </tx:attributes>
    </tx:advice>
    
    <!--AOP配置（织入）-->
    <aop:config>
        <aop:pointcut id="pt" expression="execution(* com.test.com.test.service.*.*(..))"/>
        <aop:advisor advice-ref="txAdvice" pointcut-ref="pt"/>
    </aop:config>

</beans>
